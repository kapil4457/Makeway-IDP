# Step-2 worker is stdlib-only (boto3 ships with the Lambda runtime) — it talks
# to the control plane and the exposed Kubernetes API over HTTPS with urllib, and
# to AWS via boto3. No third-party packages to bundle.
