# Golden-path Deployment for the test-service-8-2 service.
# Shared across environments — the envs/<env>/ overrides patch the image tag per
# environment. Makeway CI rewrites those patches after every build on main.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: test-service-8-2
  labels:
    app: test-service-8-2
    managed-by: makeway
spec:
  replicas: 1
  selector:
    matchLabels:
      app: test-service-8-2
  template:
    metadata:
      labels:
        app: test-service-8-2
    spec:
      containers:
        - name: test-service-8-2
          image: makeway-placeholder/test-service-8-2:pending-first-build
          ports:
            - containerPort: 3000
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi