# Service for the test-service-8-2 golden-path service.
apiVersion: v1
kind: Service
metadata:
  name: test-service-8-2
  labels:
    app: test-service-8-2
    managed-by: makeway
spec:
  selector:
    app: test-service-8-2
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP