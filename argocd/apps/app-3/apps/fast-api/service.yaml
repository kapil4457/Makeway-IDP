# Service for the fast-api golden-path service.
apiVersion: v1
kind: Service
metadata:
  name: fast-api
  labels:
    app: fast-api
    managed-by: makeway
spec:
  selector:
    app: fast-api
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP