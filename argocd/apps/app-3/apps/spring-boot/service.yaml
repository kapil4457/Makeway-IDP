# Service for the spring-boot golden-path service.
apiVersion: v1
kind: Service
metadata:
  name: spring-boot
  labels:
    app: spring-boot
    managed-by: makeway
spec:
  selector:
    app: spring-boot
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP