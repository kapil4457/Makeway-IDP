# Service for the python-service golden-path service.
apiVersion: v1
kind: Service
metadata:
  name: python-service
  labels:
    app: python-service
    managed-by: makeway
spec:
  selector:
    app: python-service
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP