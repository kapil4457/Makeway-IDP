# Service for the test-svc golden-path service.
apiVersion: v1
kind: Service
metadata:
  name: test-svc
  labels:
    app: test-svc
    managed-by: makeway
spec:
  selector:
    app: test-svc
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP