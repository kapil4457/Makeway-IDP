# Service for the node-svc golden-path service.
apiVersion: v1
kind: Service
metadata:
  name: node-svc
  labels:
    app: node-svc
    managed-by: makeway
spec:
  selector:
    app: node-svc
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP