# Service for the fast-api-2 golden-path service.
apiVersion: v1
kind: Service
metadata:
  name: fast-api-2
  labels:
    app: fast-api-2
    managed-by: makeway
spec:
  selector:
    app: fast-api-2
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP