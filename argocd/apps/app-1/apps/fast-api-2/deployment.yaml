# Golden-path Deployment for the fast-api-2 service.
# Shared across environments — the envs/<env>/ overrides patch the image tag per
# environment. Makeway CI rewrites those patches after every build on main.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: fast-api-2
  labels:
    app: fast-api-2
    managed-by: makeway
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fast-api-2
  template:
    metadata:
      labels:
        app: fast-api-2
    spec:
      containers:
        - name: fast-api-2
          image: makeway-placeholder/fast-api-2:pending-first-build
          ports:
            - containerPort: 8000
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi