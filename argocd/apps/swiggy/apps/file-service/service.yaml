# Service for the file-service golden-path service.
apiVersion: v1
kind: Service
metadata:
  name: file-service
  labels:
    app: file-service
    managed-by: makeway
spec:
  selector:
    app: file-service
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP